"use client";
import { useEffect, useState } from "react";
import "../globals.css";
type Row=Record<string,unknown>;
export default function InvestmentOsPage(){const[rules,setRules]=useState<Row[]>([]),[q,setQ]=useState(""),[out,setOut]=useState(""),[busy,setBusy]=useState(false);useEffect(()=>{fetch("/api/knowledge-universe").then(r=>r.json()).then(d=>setRules(d.규칙라이브러리||[])).catch(()=>{})},[]);async function ask(){if(!q.trim())return;setBusy(true);setOut("");try{const r=await fetch("/api/investment-decision",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({question:q,knowledge:rules})});const d=await r.json();setOut(d.answer||d.error||"응답을 받지 못했습니다.")}catch{setOut("AI 연결에 실패했습니다.")}finally{setBusy(false)}}return <main className="os-page"><a className="os-back" href="/">← AI 투자 도구 MASTER</a><header className="os-intro"><span>INVESTMENT DECISION OS · V1.2</span><h1>투자 판단 OS</h1></header><section className="os-flow"><div><b>NOW</b><span>→</span><b>SIGNAL</b><span>→</span><b>ME</b><span>→</span><b>DO</b></div><small>COLLECT → DISTILL → CONNECT → DECIDE → LEARN ↺</small></section><section className="os-panel"><span>DECIDE</span><h2>지금, 무엇을 판단하나요?</h2><textarea className="os-decide-input" value={q} onChange={e=>setQ(e.target.value)} placeholder="예: 지금 서울 정비사업에 들어가도 될까?"/><button className="os-action" onClick={ask} disabled={busy}>{busy?"판단 중…":"판단 요청"}</button>{out&&<article className="decision-card"><span>INVESTMENT DECISION ENGINE</span><pre>{out}</pre></article>}</section><section className="os-universe"><span>KNOWLEDGE UNIVERSE</span><h2>연결된 지식 카드 {rules.length}개</h2><p>Notion의 규칙·원칙·인물 알고리즘을 판단 근거로 사용합니다.</p></section></main>}
